int allIndexes(int input[],int size,int x){
	if(size==0){
		return 0;
	}
	if(input[0]==x){
		int ans=allIndexes(input+1,size-1,x);
		return ans+1;
	}
	else{
		return allIndexes(input+1,size-1,x);
	}
}
